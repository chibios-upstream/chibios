<?xml version="1.0" encoding="UTF-8"?>
<module xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:noNamespaceSchemaLocation="http://www.chibios.org/xml/schema/ccode/modules.xsd"
  name="hal_posix_tty_sio" descr="POSIX TTY over SIO Driver"
  headerpath="." sourcepath="." editcode="false">
  <brief>POSIX terminal wrapper over an SIO driver.</brief>
  <details><![CDATA[This module implements a fixed-storage POSIX-style
    terminal line discipline layered over a generic SIO driver. It provides
    canonical input editing, local echo, buffered output, software flow
    control, and typed terminal control operations without dynamic memory
    allocation. Non-canonical input supports all four VMIN/VTIME combinations,
    with VTIME expressed in tenths of a second. Reads return when the requested
    byte count is reached even if it is smaller than VMIN. A read timeout
    returns zero bytes if no input was transferred; stmGet() reports
    STM_TIMEOUT instead of STM_RESET. Each read snapshots its completion
    policy on entry; attribute changes affect subsequent reads and incoming
    character processing, not the active read's minimum or timeout.
    Canonical reads ignore VMIN and VTIME; the data format is fixed to CS8,
    CREAD, and CLOCAL. Speed fields report the
    SIO default and are not used to reconfigure the transport. Received
    transport errors are cleared and ignored, error-related input flags are
    not supported. A blocked read returns zero bytes when the driver is
    stopped or when a terminal signal flushes the input queue, pending signal
    flags allow distinguishing an interrupted read from an end-of-file
    condition. Concurrent drain operations wait for the same physical TX-idle
    condition and are all released on completion or reset. Stopping the
    driver during active blocking I/O operations is not supported. Erasing a
    tabulation character does not restore the previous column. Editing is
    byte-oriented, IUTF8 is not supported. Buffered canonical records retain
    their boundaries after switching to non-canonical mode and can end a read
    before its minimum is reached.]]></details>
  <imports>
    <import>hal_sio.xml</import>
    <import>hal_tty.xml</import>
  </imports>
  <public>
    <includes_always>
      <include style="regular">hal.h</include>
      <include style="angular">sys/termios.h</include>
      <include style="angular">sys/ioctl.h</include>
    </includes_always>
    <definitions_early>
      <group description="Supported terminal attribute masks">
        <define name="PTTY_SUPPORTED_IFLAGS"
          value="(ISTRIP | INLCR | IGNCR | ICRNL | IXON | IMAXBEL)">
          <brief>Supported input attribute flags.</brief>
        </define>
        <define name="PTTY_SUPPORTED_OFLAGS" value="(OPOST | ONLCR)">
          <brief>Supported output attribute flags.</brief>
        </define>
        <define name="PTTY_REQUIRED_CFLAGS"
          value="(CS8 | CREAD | CLOCAL)">
          <brief>Required control attribute value.</brief>
        </define>
        <define name="PTTY_SUPPORTED_LFLAGS"
          value="(ISIG | ICANON | ECHO | ECHOE | ECHOK | ECHONL | NOFLSH | ECHOCTL)">
          <brief>Supported local attribute flags.</brief>
        </define>
      </group>
      <group description="Terminal signal flags">
        <define name="PTTY_SIGNAL_NONE" value="0U">
          <brief>No pending terminal signal.</brief>
        </define>
        <define name="PTTY_SIGNAL_INTR" value="(1U &lt;&lt; 0)">
          <brief>Interrupt character received.</brief>
        </define>
        <define name="PTTY_SIGNAL_QUIT" value="(1U &lt;&lt; 1)">
          <brief>Quit character received.</brief>
        </define>
        <define name="PTTY_SIGNAL_SUSP" value="(1U &lt;&lt; 2)">
          <brief>Suspend character received.</brief>
        </define>
        <define name="PTTY_SIGNAL_ALL"
          value="(PTTY_SIGNAL_INTR | PTTY_SIGNAL_QUIT | PTTY_SIGNAL_SUSP)">
          <brief>Mask of all terminal signals.</brief>
        </define>
      </group>
    </definitions_early>
    <configs>
      <config name="PTTY_DEFAULT_COLUMNS" default="80U">
        <brief>Default terminal width for each terminal instance.</brief>
      </config>
      <config name="PTTY_DEFAULT_ROWS" default="24U">
        <brief>Default terminal height for each terminal instance.</brief>
      </config>
      <config name="PTTY_INPUT_BUFFER_SIZE" default="128U">
        <brief>Input ring size for each terminal instance.</brief>
        <details><![CDATA[One slot is reserved in canonical mode so that a
          full editing line can still be terminated by newline or EOF.]]></details>
      </config>
      <config name="PTTY_OUTPUT_BUFFER_SIZE" default="128U">
        <brief>Application output queue size for each terminal instance.</brief>
      </config>
      <config name="PTTY_ECHO_BUFFER_SIZE" default="8U">
        <brief>Echo and control queue size for each terminal instance.</brief>
      </config>
    </configs>
    <definitions_late>
      <group description="Derived buffer sizes">
        <define name="PTTY_INPUT_BOUNDARY_MAP_SIZE"
          value="((PTTY_INPUT_BUFFER_SIZE + 7U) / 8U)">
          <brief>Input boundary bitmap size in bytes.</brief>
        </define>
      </group>
      <verbatim><![CDATA[
#if HAL_USE_SIO != TRUE
#error "POSIX TTY over SIO requires HAL_USE_SIO"
#endif

#if PTTY_DEFAULT_COLUMNS == 0U
#error "PTTY_DEFAULT_COLUMNS must be greater than zero"
#endif

#if PTTY_DEFAULT_ROWS == 0U
#error "PTTY_DEFAULT_ROWS must be greater than zero"
#endif

#if PTTY_INPUT_BUFFER_SIZE < 2U
#error "PTTY_INPUT_BUFFER_SIZE must be at least two"
#endif

#if PTTY_OUTPUT_BUFFER_SIZE == 0U
#error "PTTY_OUTPUT_BUFFER_SIZE must be greater than zero"
#endif

#if PTTY_ECHO_BUFFER_SIZE == 0U
#error "PTTY_ECHO_BUFFER_SIZE must be greater than zero"
#endif]]></verbatim>
    </definitions_late>
    <types>
      <verbatim><![CDATA[
/**
 * @brief       Terminal signal flags type.
 * @note        Pending flags can be fetched and cleared from the inherited
 *              driver callback using @p pttyGetAndClearSignalsX().
 */
typedef uint32_t pttysignals_t;

/**
 * @brief       Canonical-aware input ring.
 * @details     Canonical record endings are represented by one bit per data
 *              slot. A marked zero byte is an internal EOF placeholder.
 */
typedef struct {
  threads_queue_t           waiting;
  uint8_t                   buffer[PTTY_INPUT_BUFFER_SIZE];
  uint8_t                   boundaries[PTTY_INPUT_BOUNDARY_MAP_SIZE];
  size_t                    read;
  size_t                    write;
  size_t                    committed;
  size_t                    editing;
} ptty_input_queue_t;]]></verbatim>
      <class type="regular" name="hal_posix_tty_sio" namespace="ptty"
        ancestorname="hal_cb_driver" descr="POSIX TTY over SIO driver">
        <brief>POSIX terminal line discipline wrapper over SIO.</brief>
        <details><![CDATA[The class owns the callback of the associated SIO
          driver while started. Application output uses an output queue;
          terminal echo uses a separate higher-priority output queue; both
          queues are held while output is stopped by flow control. A dedicated
          one-character slot holds a pending flow-control character requested
          by @p ttyFlow(), it is transmitted even while output is stopped.
          The inherited callback is reserved for
          terminal-generated integration notifications and is invoked from
          ISR context outside system locks. Echo is generated without blocking
          in ISR context, therefore @p PTTY_ECHO_BUFFER_SIZE should cover the
          longest editing sequence that must be preserved. If this queue is
          full, additional echo bytes are discarded; the received input is
          not discarded for that reason. TX backpressure can therefore leave
          the displayed line out of sync with the input buffer.
          The SIO transport must re-arm enabled TX-space and TX-end events
          when new frames are written, even if its logical enabled-event mask
          is unchanged. TX-idle state must remain observable until another
          transmission starts; this wrapper does not clear TX-end events.
          All queue storage is
          embedded in each instance and sized by module configuration
          options.]]></details>
        <implements>
          <if name="tty">
            <method shortname="write">
              <implementation><![CDATA[

return __ptty_write(self, bp, n);]]></implementation>
            </method>
            <method shortname="read">
              <implementation><![CDATA[

msg_t msg;

return __ptty_read(self, bp, n, &msg);]]></implementation>
            </method>
            <method shortname="put">
              <implementation><![CDATA[

if (__ptty_write(self, &b, 1U) == 1U) {
  return STM_OK;
}

return STM_RESET;]]></implementation>
            </method>
            <method shortname="get">
              <implementation><![CDATA[
uint8_t b;
msg_t msg;

if (__ptty_read(self, &b, 1U, &msg) == 1U) {
  return (int)b;
}

return msg == MSG_TIMEOUT ? STM_TIMEOUT : STM_RESET;]]></implementation>
            </method>
            <method shortname="unget">
              <implementation><![CDATA[

(void)self;
(void)b;

return STM_RESET;]]></implementation>
            </method>
            <method shortname="getattr">
              <implementation><![CDATA[

chDbgCheck(attrp != NULL);

chSysLock();
if (self->state != HAL_DRV_STATE_READY) {
  chSysUnlock();
  return HAL_RET_INV_STATE;
}
*attrp = self->attributes;
chSysUnlock();

return HAL_RET_SUCCESS;]]></implementation>
            </method>
            <method shortname="setattr">
              <implementation><![CDATA[
msg_t msg;

chDbgCheck(attrp != NULL);

if ((action != TCSANOW) &&
    (action != TCSADRAIN) &&
    (action != TCSAFLUSH)) {
  return HAL_RET_CONFIG_ERROR;
}
if (!__ptty_attributes_valid(self, attrp)) {
  return HAL_RET_CONFIG_ERROR;
}

if ((action == TCSADRAIN) || (action == TCSAFLUSH)) {
  msg = __ptty_drain(self);
  if (msg != HAL_RET_SUCCESS) {
    return msg;
  }
}

chSysLock();
if (self->state != HAL_DRV_STATE_READY) {
  chSysUnlock();
  return HAL_RET_INV_STATE;
}
if (action == TCSAFLUSH) {
  __ptty_input_reset_i(self);
}
__ptty_apply_attributes_i(self, attrp);
chSchRescheduleS();
chSysUnlock();

return HAL_RET_SUCCESS;]]></implementation>
            </method>
            <method shortname="drain">
              <implementation><![CDATA[

return __ptty_drain(self);]]></implementation>
            </method>
            <method shortname="flush">
              <implementation><![CDATA[

if ((queues != TCIFLUSH) &&
    (queues != TCOFLUSH) &&
    (queues != TCIOFLUSH)) {
  return HAL_RET_CONFIG_ERROR;
}

chSysLock();
if (self->state != HAL_DRV_STATE_READY) {
  chSysUnlock();
  return HAL_RET_INV_STATE;
}
if ((queues == TCIFLUSH) || (queues == TCIOFLUSH)) {
  __ptty_input_reset_i(self);
}
if ((queues == TCOFLUSH) || (queues == TCIOFLUSH)) {
  oqResetI(&self->oqueue);
  qResetI(&self->equeue);
  __ptty_push_output_i(self);
}
chSchRescheduleS();
chSysUnlock();

return HAL_RET_SUCCESS;]]></implementation>
            </method>
            <method shortname="flow">
              <implementation><![CDATA[
unsigned index;
uint8_t b;

switch (action) {
case TCOOFF:
  chSysLock();
  if (self->state != HAL_DRV_STATE_READY) {
    chSysUnlock();
    return HAL_RET_INV_STATE;
  }
  self->output_stopped = true;
  __ptty_update_tx_i(self);
  chSysUnlock();
  return HAL_RET_SUCCESS;
case TCOON:
  chSysLock();
  if (self->state != HAL_DRV_STATE_READY) {
    chSysUnlock();
    return HAL_RET_INV_STATE;
  }
  self->output_stopped = false;
  __ptty_push_output_i(self);
  chSchRescheduleS();
  chSysUnlock();
  return HAL_RET_SUCCESS;
case TCIOFF:
  index = VSTOP;
  break;
case TCION:
  index = VSTART;
  break;
default:
  return HAL_RET_CONFIG_ERROR;
}

chSysLock();
if (self->state != HAL_DRV_STATE_READY) {
  chSysUnlock();
  return HAL_RET_INV_STATE;
}
b = self->attributes.c_cc[index];
if (b != (uint8_t)_POSIX_VDISABLE) {
  self->flow_pending = true;
  self->flow_char    = b;
  __ptty_push_output_i(self);
  chSchRescheduleS();
}
chSysUnlock();

return HAL_RET_SUCCESS;]]></implementation>
            </method>
            <method shortname="getwinsize">
              <implementation><![CDATA[

chDbgCheck(sizep != NULL);

chSysLock();
if (self->state != HAL_DRV_STATE_READY) {
  chSysUnlock();
  return HAL_RET_INV_STATE;
}
*sizep = self->winsize;
chSysUnlock();

return HAL_RET_SUCCESS;]]></implementation>
            </method>
            <method shortname="setwinsize">
              <implementation><![CDATA[

chDbgCheck(sizep != NULL);

chSysLock();
if (self->state != HAL_DRV_STATE_READY) {
  chSysUnlock();
  return HAL_RET_INV_STATE;
}
self->winsize = *sizep;
chSysUnlock();

return HAL_RET_SUCCESS;]]></implementation>
            </method>
          </if>
        </implements>
        <fields>
          <field name="oqueue" ctype="output_queue_t">
            <brief>Application output queue.</brief>
          </field>
          <field name="equeue" ctype="plain_queue_t">
            <brief>High-priority echo and control output queue.</brief>
          </field>
          <field name="siop" ctype="hal_sio_driver_c$I*">
            <brief>Associated SIO transport.</brief>
          </field>
          <field name="attributes" ctype="struct termios">
            <brief>Active POSIX terminal attributes.</brief>
          </field>
          <field name="readf"
            ctype="size_t (*$N)(hal_posix_tty_sio_c *self, uint8_t *bp, size_t n, msg_t *msgp)">
            <brief>Selected read handler, called with the system locked.</brief>
          </field>
          <field name="winsize" ctype="struct winsize">
            <brief>Active terminal window size.</brief>
          </field>
          <field name="drainsync" ctype="threads_queue_t">
            <brief>Threads waiting for output drain completion.</brief>
          </field>
          <field name="signals" ctype="volatile pttysignals_t">
            <brief>Pending terminal signal flags.</brief>
          </field>
          <field name="output_stopped" ctype="bool">
            <brief>Application output software-flow-control state.</brief>
          </field>
          <field name="drain_waiting" ctype="bool">
            <brief>Output drain synchronization state.</brief>
          </field>
          <field name="flow_pending" ctype="bool">
            <brief>Pending flow-control character state.</brief>
          </field>
          <field name="flow_char" ctype="uint8_t">
            <brief>Pending flow-control character value.</brief>
          </field>
          <field name="iqueue" ctype="ptty_input_queue_t">
            <brief>Canonical-aware input queue.</brief>
          </field>
          <field name="obuffer"
            ctype="uint8_t$I$N[PTTY_OUTPUT_BUFFER_SIZE]">
            <brief>Embedded application output storage.</brief>
          </field>
          <field name="ebuffer"
            ctype="uint8_t$I$N[PTTY_ECHO_BUFFER_SIZE]">
            <brief>Embedded echo and control output storage.</brief>
          </field>
        </fields>
        <methods>
          <objinit callsuper="true">
            <param name="siop" ctype="hal_sio_driver_c *" dir="in">
              Pointer to the underlying SIO driver.
            </param>
            <implementation><![CDATA[

chDbgCheck(siop != NULL);

__ptty_input_init(&self->iqueue);
oqObjectInit(&self->oqueue, self->obuffer, sizeof self->obuffer,
             __ptty_onotify, self);
qObjectInit(&self->equeue, self->ebuffer, sizeof self->ebuffer);
chThdQueueObjectInit(&self->drainsync);
self->siop           = siop;
self->signals        = PTTY_SIGNAL_NONE;
self->output_stopped = false;
self->drain_waiting  = false;
self->flow_pending   = false;
self->flow_char      = 0U;
__ptty_attributes_default(self);
self->winsize.ws_row    = PTTY_DEFAULT_ROWS;
self->winsize.ws_col    = PTTY_DEFAULT_COLUMNS;
self->winsize.ws_xpixel = 0U;
self->winsize.ws_ypixel = 0U;]]></implementation>
          </objinit>
          <dispose>
            <implementation><![CDATA[ ]]></implementation>
          </dispose>
          <regular>
            <method name="pttyReset" ctype="msg_t">
              <brief>Restores the terminal to its initial state.</brief>
              <details><![CDATA[All pending input, output, echo, flow-control,
                and signal state is discarded. Terminal attributes and window
                size are restored to their configured defaults. The associated
                SIO driver remains started and configured.]]></details>
              <return>The operation status.</return>
              <retval value="HAL_RET_SUCCESS">if the terminal was reset.</retval>
              <retval value="HAL_RET_INV_STATE">if the terminal is not
                started.</retval>
              <api />
              <implementation><![CDATA[

chDbgCheck(self != NULL);

chSysLock();
if (self->state != HAL_DRV_STATE_READY) {
  chSysUnlock();
  return HAL_RET_INV_STATE;
}

__ptty_input_reset_i(self);
oqResetI(&self->oqueue);
qResetI(&self->equeue);
__ptty_reset_drain_i(self);
while (!sioIsRXEmptyX(self->siop)) {
  (void)sioGetX(self->siop);
}
/* TX_END also represents the physical idle state on some SIO ports. It
   must remain observable by drain until the next transmit starts.*/
(void)sioGetAndClearEventsX(self->siop, SIO_EV_ALL_EVENTS & ~SIO_EV_TX_END);
self->signals        = PTTY_SIGNAL_NONE;
self->output_stopped = false;
self->flow_pending   = false;
self->flow_char      = 0U;
__ptty_attributes_default(self);
self->winsize.ws_row    = PTTY_DEFAULT_ROWS;
self->winsize.ws_col    = PTTY_DEFAULT_COLUMNS;
self->winsize.ws_xpixel = 0U;
self->winsize.ws_ypixel = 0U;
__ptty_update_tx_i(self);
chSchRescheduleS();
chSysUnlock();

return HAL_RET_SUCCESS;]]></implementation>
            </method>
          </regular>
          <inline>
            <method name="pttyGetAndClearSignalsX" ctype="pttysignals_t">
              <brief>Gets and clears pending terminal signal flags.</brief>
              <param name="mask" ctype="pttysignals_t" dir="in">
                Mask of signals to be returned and cleared.
              </param>
              <return>The selected pending signal flags.</return>
              <xclass />
              <implementation><![CDATA[
pttysignals_t signals;
syssts_t sts;

sts = chSysGetStatusAndLockX();
signals = self->signals & mask;
self->signals &= ~mask;
chSysRestoreStatusX(sts);

return signals;]]></implementation>
            </method>
          </inline>
          <override>
            <method shortname="start">
              <implementation><![CDATA[
msg_t msg;

msg = drvStart(self->siop, config);
if (msg != HAL_RET_SUCCESS) {
  return msg;
}

chSysLock();
__ptty_input_reset_i(self);
oqResetI(&self->oqueue);
qResetI(&self->equeue);
__ptty_reset_drain_i(self);
self->config         = self->siop->config;
self->signals        = PTTY_SIGNAL_NONE;
self->output_stopped = false;
self->flow_pending   = false;
drvSetArgumentX(self->siop, self);
drvSetCallbackX(self->siop, __ptty_sio_cb);
sioWriteEnableFlagsX(self->siop,
                     SIO_EV_ALL_ERRORS |
                     SIO_EV_RX_NOTEMPTY |
                     SIO_EV_RX_IDLE);
chSchRescheduleS();
chSysUnlock();

return HAL_RET_SUCCESS;]]></implementation>
            </method>
            <method shortname="stop">
              <implementation><![CDATA[

chSysLock();
sioWriteEnableFlagsX(self->siop, SIO_EV_NONE);
drvSetCallbackX(self->siop, NULL);
drvSetArgumentX(self->siop, NULL);
__ptty_input_reset_i(self);
oqResetI(&self->oqueue);
qResetI(&self->equeue);
__ptty_reset_drain_i(self);
self->signals        = PTTY_SIGNAL_NONE;
self->output_stopped = false;
self->flow_pending   = false;
chSchRescheduleS();
chSysUnlock();

drvStop(self->siop);]]></implementation>
            </method>
            <method shortname="setcfg">
              <implementation><![CDATA[

if (drvSetCfgX(self->siop, config) != HAL_RET_SUCCESS) {
  return NULL;
}

return self->siop->config;]]></implementation>
            </method>
            <method shortname="selcfg">
              <implementation><![CDATA[

return drvSelectCfgX(self->siop, cfgnum);]]></implementation>
            </method>
          </override>
        </methods>
      </class>
    </types>
  </public>
  <private>
    <includes_always>
      <include style="regular">hal_posix_tty_sio.h</include>
    </includes_always>
    <types>
      <verbatim><![CDATA[
static void __ptty_update_tx_i(hal_posix_tty_sio_c *self);
static void __ptty_push_output_i(hal_posix_tty_sio_c *self);
static void __ptty_select_read(hal_posix_tty_sio_c *self);

static void __ptty_input_init(ptty_input_queue_t *iqp) {
  size_t i;

  chThdQueueObjectInit(&iqp->waiting);
  iqp->read      = 0U;
  iqp->write     = 0U;
  iqp->committed = 0U;
  iqp->editing   = 0U;
  for (i = 0U; i < PTTY_INPUT_BOUNDARY_MAP_SIZE; i++) {
    iqp->boundaries[i] = 0U;
  }
}

static void __ptty_attributes_default(hal_posix_tty_sio_c *self) {
  struct termios *attrp;
  size_t i;

  attrp = &self->attributes;
  attrp->c_iflag  = ICRNL | IXON | IMAXBEL;
  attrp->c_oflag  = OPOST | ONLCR;
  attrp->c_cflag  = PTTY_REQUIRED_CFLAGS;
  attrp->c_lflag  = ISIG | ICANON | ECHO | ECHOE | ECHOK | ECHOCTL;
  for (i = 0U; i < NCCS; i++) {
    attrp->c_cc[i] = 0U;
  }
  attrp->c_cc[VINTR]  = 0x03U;
  attrp->c_cc[VQUIT]  = 0x1CU;
  attrp->c_cc[VERASE] = 0x7FU;
  attrp->c_cc[VKILL]  = 0x15U;
  attrp->c_cc[VEOF]   = 0x04U;
  attrp->c_cc[VTIME]  = 0U;
  attrp->c_cc[VMIN]   = 1U;
  attrp->c_cc[VSTART] = 0x11U;
  attrp->c_cc[VSTOP]  = 0x13U;
  attrp->c_cc[VSUSP]  = 0x1AU;
  attrp->c_ispeed     = (speed_t)SIO_DEFAULT_BITRATE;
  attrp->c_ospeed     = (speed_t)SIO_DEFAULT_BITRATE;
  __ptty_select_read(self);
}

/* Validation must see the full result even with 16-bit RT time types and
   a high timer frequency; only validated read intervals may be narrowed.*/
static uint64_t __ptty_vtime_interval(const struct termios *attrp) {

  return ((uint64_t)attrp->c_cc[VTIME] * CH_CFG_ST_FREQUENCY + 9U) / 10U;
}

static bool __ptty_attributes_valid(hal_posix_tty_sio_c *self,
                                    const struct termios *attrp) {

  if ((attrp->c_iflag & ~PTTY_SUPPORTED_IFLAGS) != 0U) {
    return false;
  }
  if ((attrp->c_oflag & ~PTTY_SUPPORTED_OFLAGS) != 0U) {
    return false;
  }
  if (attrp->c_cflag != PTTY_REQUIRED_CFLAGS) {
    return false;
  }
  if ((attrp->c_lflag & ~PTTY_SUPPORTED_LFLAGS) != 0U) {
    return false;
  }
  /* Canonical input ignores VTIME. Non-canonical intervals must fit
     in the RT timer without becoming TIME_IMMEDIATE or TIME_INFINITE.*/
  if (((attrp->c_lflag & ICANON) == 0U) &&
      (__ptty_vtime_interval(attrp) > (uint64_t)TIME_MAX_INTERVAL)) {
    return false;
  }
  if ((attrp->c_ispeed != self->attributes.c_ispeed) ||
      (attrp->c_ospeed != self->attributes.c_ospeed)) {
    return false;
  }

  return true;
}

static bool __ptty_cc_equal_i(const hal_posix_tty_sio_c *self,
                              unsigned index,
                              uint8_t b) {
  cc_t c;

  c = self->attributes.c_cc[index];
  return (c != (cc_t)_POSIX_VDISABLE) && (b == (uint8_t)c);
}

static size_t __ptty_input_used_i(const hal_posix_tty_sio_c *self) {

  return self->iqueue.committed + self->iqueue.editing;
}

static size_t __ptty_input_advance(size_t pos) {

  pos++;
  if (pos >= PTTY_INPUT_BUFFER_SIZE) {
    pos = 0U;
  }

  return pos;
}

static size_t __ptty_input_retreat(size_t pos) {

  if (pos == 0U) {
    pos = PTTY_INPUT_BUFFER_SIZE;
  }

  return pos - 1U;
}

static bool __ptty_input_is_boundary_i(const ptty_input_queue_t *iqp,
                                       size_t pos) {
  uint8_t mask;

  mask = (uint8_t)(1U << (pos & 7U));
  return (iqp->boundaries[pos >> 3] & mask) != 0U;
}

static void __ptty_input_set_boundary_i(ptty_input_queue_t *iqp,
                                        size_t pos) {
  uint8_t mask;

  mask = (uint8_t)(1U << (pos & 7U));
  iqp->boundaries[pos >> 3] |= mask;
}

static void __ptty_input_clear_boundary_i(ptty_input_queue_t *iqp,
                                          size_t pos) {
  uint8_t mask;

  mask = (uint8_t)(1U << (pos & 7U));
  iqp->boundaries[pos >> 3] &= (uint8_t)~mask;
}

static void __ptty_input_wakeup_i(hal_posix_tty_sio_c *self) {

  chThdDequeueNextI(&self->iqueue.waiting, MSG_OK);
}

static void __ptty_input_reset_i(hal_posix_tty_sio_c *self) {
  size_t i;

  self->iqueue.read      = 0U;
  self->iqueue.write     = 0U;
  self->iqueue.committed = 0U;
  self->iqueue.editing   = 0U;
  for (i = 0U; i < PTTY_INPUT_BOUNDARY_MAP_SIZE; i++) {
    self->iqueue.boundaries[i] = 0U;
  }
  chThdDequeueAllI(&self->iqueue.waiting, MSG_RESET);
}

static bool __ptty_input_append_i(hal_posix_tty_sio_c *self,
                                  uint8_t b,
                                  bool boundary,
                                  bool committed) {
  ptty_input_queue_t *iqp;

  iqp = &self->iqueue;
  if (__ptty_input_used_i(self) >= PTTY_INPUT_BUFFER_SIZE) {
    return false;
  }

  iqp->buffer[iqp->write] = b;
  __ptty_input_clear_boundary_i(iqp, iqp->write);
  if (boundary) {
    __ptty_input_set_boundary_i(iqp, iqp->write);
  }
  iqp->write = __ptty_input_advance(iqp->write);
  if (committed) {
    iqp->committed++;
    __ptty_input_wakeup_i(self);
  }
  else {
    iqp->editing++;
  }

  return true;
}

static void __ptty_input_commit_i(hal_posix_tty_sio_c *self) {

  if (self->iqueue.editing > 0U) {
    self->iqueue.committed += self->iqueue.editing;
    self->iqueue.editing = 0U;
    __ptty_input_wakeup_i(self);
  }
}

static void __ptty_echo_raw_i(hal_posix_tty_sio_c *self, uint8_t b) {

  (void)qPutI(&self->equeue, b);
}

static void __ptty_echo_output_i(hal_posix_tty_sio_c *self, uint8_t b) {

  if (((self->attributes.c_oflag & (OPOST | ONLCR)) ==
       (OPOST | ONLCR)) && (b == (uint8_t)'\n')) {
    __ptty_echo_raw_i(self, (uint8_t)'\r');
  }
  __ptty_echo_raw_i(self, b);
}

static void __ptty_echo_char_i(hal_posix_tty_sio_c *self, uint8_t b) {

  if ((self->attributes.c_lflag & ECHO) == 0U) {
    if (((self->attributes.c_lflag & (ECHONL | ICANON)) ==
         (ECHONL | ICANON)) && (b == (uint8_t)'\n')) {
      __ptty_echo_output_i(self, b);
    }
    return;
  }

  if (((self->attributes.c_lflag & ECHOCTL) != 0U) &&
      (((b < 0x20U) && (b != (uint8_t)'\n') &&
        (b != (uint8_t)'\r') && (b != (uint8_t)'\t')) ||
       (b == 0x7FU))) {
    __ptty_echo_raw_i(self, (uint8_t)'^');
    if (b == 0x7FU) {
      b = (uint8_t)'?';
    }
    else {
      b = (uint8_t)(b + (uint8_t)'@');
    }
  }
  __ptty_echo_output_i(self, b);
}

static void __ptty_echo_erase_i(hal_posix_tty_sio_c *self, uint8_t b) {
  unsigned columns;

  if ((self->attributes.c_lflag & ECHO) == 0U) {
    return;
  }
  if ((self->attributes.c_lflag & ECHOE) == 0U) {
    __ptty_echo_char_i(self, self->attributes.c_cc[VERASE]);
    return;
  }

  columns = 1U;
  if (((self->attributes.c_lflag & ECHOCTL) != 0U) &&
      (((b < 0x20U) && (b != (uint8_t)'\t')) || (b == 0x7FU))) {
    columns = 2U;
  }
  while (columns > 0U) {
    __ptty_echo_raw_i(self, (uint8_t)'\b');
    __ptty_echo_raw_i(self, (uint8_t)' ');
    __ptty_echo_raw_i(self, (uint8_t)'\b');
    columns--;
  }
}

static void __ptty_input_full_i(hal_posix_tty_sio_c *self) {

  if ((self->attributes.c_iflag & IMAXBEL) != 0U) {
    __ptty_echo_raw_i(self, (uint8_t)'\a');
  }
}

static void __ptty_input_erase_i(hal_posix_tty_sio_c *self) {
  ptty_input_queue_t *iqp;
  uint8_t b;

  iqp = &self->iqueue;
  if (iqp->editing == 0U) {
    return;
  }

  iqp->write = __ptty_input_retreat(iqp->write);
  iqp->editing--;
  b = iqp->buffer[iqp->write];
  __ptty_input_clear_boundary_i(iqp, iqp->write);
  __ptty_echo_erase_i(self, b);
}

static void __ptty_input_kill_i(hal_posix_tty_sio_c *self) {
  ptty_input_queue_t *iqp;

  iqp = &self->iqueue;
  while (iqp->editing > 0U) {
    iqp->write = __ptty_input_retreat(iqp->write);
    iqp->editing--;
    __ptty_input_clear_boundary_i(iqp, iqp->write);
  }
  if (((self->attributes.c_lflag & ECHO) != 0U) &&
      ((self->attributes.c_lflag & ECHOK) != 0U)) {
    __ptty_echo_output_i(self, (uint8_t)'\n');
  }
}

static void __ptty_input_eof_i(hal_posix_tty_sio_c *self) {

  if (!__ptty_input_append_i(self, 0U, true, false)) {
    __ptty_input_full_i(self);
    return;
  }
  __ptty_input_commit_i(self);
}

static void __ptty_flush_signal_i(hal_posix_tty_sio_c *self) {

  if ((self->attributes.c_lflag & NOFLSH) == 0U) {
    __ptty_input_reset_i(self);
    oqResetI(&self->oqueue);
    qResetI(&self->equeue);
    self->output_stopped = false;
  }
}

static pttysignals_t __ptty_process_input_i(hal_posix_tty_sio_c *self,
                                            uint8_t b) {
  tcflag_t iflag;
  tcflag_t lflag;

  iflag = self->attributes.c_iflag;
  lflag = self->attributes.c_lflag;

  if ((iflag & ISTRIP) != 0U) {
    b &= 0x7FU;
  }
  if (b == (uint8_t)'\r') {
    if ((iflag & IGNCR) != 0U) {
      return PTTY_SIGNAL_NONE;
    }
    if ((iflag & ICRNL) != 0U) {
      b = (uint8_t)'\n';
    }
  }
  else if ((b == (uint8_t)'\n') && ((iflag & INLCR) != 0U)) {
    b = (uint8_t)'\r';
  }

  if ((iflag & IXON) != 0U) {
    if (__ptty_cc_equal_i(self, VSTOP, b)) {
      self->output_stopped = true;
      __ptty_update_tx_i(self);
      return PTTY_SIGNAL_NONE;
    }
    if (__ptty_cc_equal_i(self, VSTART, b)) {
      self->output_stopped = false;
      __ptty_push_output_i(self);
      return PTTY_SIGNAL_NONE;
    }
  }

  if ((lflag & ISIG) != 0U) {
    if (__ptty_cc_equal_i(self, VINTR, b)) {
      __ptty_flush_signal_i(self);
      __ptty_echo_char_i(self, b);
      if ((lflag & ECHO) != 0U) {
        __ptty_echo_output_i(self, (uint8_t)'\n');
      }
      return PTTY_SIGNAL_INTR;
    }
    if (__ptty_cc_equal_i(self, VQUIT, b)) {
      __ptty_flush_signal_i(self);
      __ptty_echo_char_i(self, b);
      if ((lflag & ECHO) != 0U) {
        __ptty_echo_output_i(self, (uint8_t)'\n');
      }
      return PTTY_SIGNAL_QUIT;
    }
    if (__ptty_cc_equal_i(self, VSUSP, b)) {
      __ptty_flush_signal_i(self);
      __ptty_echo_char_i(self, b);
      if ((lflag & ECHO) != 0U) {
        __ptty_echo_output_i(self, (uint8_t)'\n');
      }
      return PTTY_SIGNAL_SUSP;
    }
  }

  if ((lflag & ICANON) == 0U) {
    if (!__ptty_input_append_i(self, b, false, true)) {
      __ptty_input_full_i(self);
      return PTTY_SIGNAL_NONE;
    }
    __ptty_echo_char_i(self, b);
    return PTTY_SIGNAL_NONE;
  }

  if (__ptty_cc_equal_i(self, VERASE, b)) {
    __ptty_input_erase_i(self);
    return PTTY_SIGNAL_NONE;
  }
  if (__ptty_cc_equal_i(self, VKILL, b)) {
    __ptty_input_kill_i(self);
    return PTTY_SIGNAL_NONE;
  }
  if (__ptty_cc_equal_i(self, VEOF, b)) {
    __ptty_input_eof_i(self);
    return PTTY_SIGNAL_NONE;
  }

  if ((b == (uint8_t)'\n') || __ptty_cc_equal_i(self, VEOL, b)) {
    if (!__ptty_input_append_i(self, b, true, false)) {
      __ptty_input_full_i(self);
      return PTTY_SIGNAL_NONE;
    }
    __ptty_input_commit_i(self);
    __ptty_echo_char_i(self, b);
    return PTTY_SIGNAL_NONE;
  }

  if ((__ptty_input_used_i(self) >= (PTTY_INPUT_BUFFER_SIZE - 1U)) ||
      !__ptty_input_append_i(self, b, false, false)) {
    __ptty_input_full_i(self);
    return PTTY_SIGNAL_NONE;
  }
  __ptty_echo_char_i(self, b);

  return PTTY_SIGNAL_NONE;
}

static bool __ptty_output_pending_i(const hal_posix_tty_sio_c *self) {

  if (self->flow_pending) {
    return true;
  }
  if (!self->output_stopped &&
      (!qIsEmptyI(&self->equeue) || !oqIsEmptyI(&self->oqueue))) {
    return true;
  }

  return false;
}

static void __ptty_update_tx_i(hal_posix_tty_sio_c *self) {
  sioevents_t current;
  sioevents_t mask;

  current = sioGetEnableFlagsX(self->siop);
  mask = current & ~(SIO_EV_TX_NOTFULL | SIO_EV_TX_END);
  if (__ptty_output_pending_i(self)) {
    mask |= SIO_EV_TX_NOTFULL;
  }
  if (self->drain_waiting) {
    mask |= SIO_EV_TX_END;
  }
  if (mask != current) {
    /* Do not re-arm an unchanged TX-end source while TC is still asserted:
       queued but flow-stopped output could otherwise cause an IRQ storm.*/
    sioWriteEnableFlagsX(self->siop, mask);
  }
}

static void __ptty_reset_drain_i(hal_posix_tty_sio_c *self) {

  self->drain_waiting = false;
  chThdDequeueAllI(&self->drainsync, MSG_RESET);
}

static void __ptty_resume_drain_i(hal_posix_tty_sio_c *self) {

  if (self->drain_waiting &&
      !self->flow_pending &&
      qIsEmptyI(&self->equeue) &&
      oqIsEmptyI(&self->oqueue) &&
      !sioIsTXOngoingX(self->siop)) {
    self->drain_waiting = false;
    chThdDequeueAllI(&self->drainsync, MSG_OK);
  }
}

static void __ptty_push_output_i(hal_posix_tty_sio_c *self) {
  msg_t msg;

  /* Late writers could reach this point through the queues callback
     after the driver has been stopped, the transport must not be
     touched in that case.*/
  if (self->state != HAL_DRV_STATE_READY) {
    return;
  }

  while (!sioIsTXFullX(self->siop)) {
    if (self->flow_pending) {
      sioPutX(self->siop, (uint_fast16_t)self->flow_char);
      self->flow_pending = false;
      continue;
    }
    if (self->output_stopped) {
      break;
    }
    msg = qGetI(&self->equeue);
    if (msg < MSG_OK) {
      msg = oqGetI(&self->oqueue);
    }
    if (msg < MSG_OK) {
      break;
    }
    sioPutX(self->siop, (uint_fast16_t)msg);
  }

  __ptty_resume_drain_i(self);
  __ptty_update_tx_i(self);
}

static void __ptty_onotify(io_queue_t *qp) {
  hal_posix_tty_sio_c *self;

  self = (hal_posix_tty_sio_c *)qGetLink(qp);
  __ptty_push_output_i(self);
}

static void __ptty_sio_cb(void *ip) {
  hal_sio_driver_c *siop;
  hal_posix_tty_sio_c *self;
  pttysignals_t signals;

  siop = (hal_sio_driver_c *)ip;
  self = (hal_posix_tty_sio_c *)drvGetArgumentX(siop);
  if (self == NULL) {
    return;
  }

  chSysLockFromISR();

  while (!sioIsRXEmptyX(siop)) {
    signals = __ptty_process_input_i(self, (uint8_t)sioGetX(siop));
    __ptty_push_output_i(self);
    if (signals != PTTY_SIGNAL_NONE) {
      self->signals |= signals;
      chSysUnlockFromISR();
      __cbdrv_invoke_cb(self);
      chSysLockFromISR();
    }
  }

  __ptty_push_output_i(self);
  /* Do not clear physical TX completion: later drain calls must still see
     idle. The TX_END interrupt is disabled when no drain is waiting.*/
  (void)sioGetAndClearEventsX(siop, SIO_EV_ALL_EVENTS & ~SIO_EV_TX_END);
  __ptty_resume_drain_i(self);
  __ptty_update_tx_i(self);

  chSysUnlockFromISR();
}

static size_t __ptty_write(hal_posix_tty_sio_c *self,
                           const uint8_t *bp,
                           size_t n) {
  tcflag_t oflag;
  size_t done;

  chDbgCheck((bp != NULL) || (n == 0U));
  if (n == 0U) {
    return 0U;
  }

  chSysLock();
  if (self->state != HAL_DRV_STATE_READY) {
    chSysUnlock();
    return 0U;
  }
  oflag = self->attributes.c_oflag;
  chSysUnlock();

  for (done = 0U; done < n; done++) {
    uint8_t b;

    b = bp[done];
    if (((oflag & (OPOST | ONLCR)) == (OPOST | ONLCR)) &&
        (b == (uint8_t)'\n')) {
      if (oqPutTimeout(&self->oqueue,
                       (uint8_t)'\r',
                       TIME_INFINITE) != MSG_OK) {
        break;
      }
    }
    if (oqPutTimeout(&self->oqueue, b, TIME_INFINITE) != MSG_OK) {
      break;
    }
  }

  return done;
}

/* Copies committed input, stopping at a record boundary or a full caller
   buffer. A true result includes an empty EOF record.*/
static bool __ptty_input_read_i(ptty_input_queue_t *iqp,
                                uint8_t **bpp,
                                size_t *np) {

  while ((*np > 0U) && (iqp->committed > 0U)) {
    bool boundary;
    uint8_t b;

    boundary = __ptty_input_is_boundary_i(iqp, iqp->read);
    b = iqp->buffer[iqp->read];
    __ptty_input_clear_boundary_i(iqp, iqp->read);
    iqp->read = __ptty_input_advance(iqp->read);
    iqp->committed--;

    if (boundary && (b == 0U)) {
      return true;
    }
    *(*bpp)++ = b;
    (*np)--;
    if (boundary) {
      return true;
    }
  }
  /* An exact-sized EOF record must not leave a spurious empty read.*/
  if ((*np == 0U) && (iqp->committed > 0U) &&
      __ptty_input_is_boundary_i(iqp, iqp->read) &&
      (iqp->buffer[iqp->read] == 0U)) {
    __ptty_input_clear_boundary_i(iqp, iqp->read);
    iqp->read = __ptty_input_advance(iqp->read);
    iqp->committed--;
  }

  return *np == 0U;
}

/* Waits against an existing deadline; bare wakeups never rearm it.*/
static msg_t __ptty_read_wait_s(hal_posix_tty_sio_c *self,
                                systime_t started,
                                sysinterval_t interval) {
  sysinterval_t timeout;
  msg_t msg;

  timeout = interval;
  if (interval != TIME_INFINITE) {
    sysinterval_t elapsed;

    elapsed = chTimeDiffX(started, chVTGetSystemTimeX());
    if (elapsed >= interval) {
      return MSG_TIMEOUT;
    }
    timeout -= elapsed;
  }
  msg = chThdEnqueueTimeoutS(&self->iqueue.waiting, timeout);
  if (self->state != HAL_DRV_STATE_READY) {
    return MSG_RESET;
  }

  return msg;
}

/* MIN = 0, TIME = 0: never waits.*/
static size_t __ptty_read_poll_s(hal_posix_tty_sio_c *self,
                                 uint8_t *bp,
                                 size_t n,
                                 msg_t *msgp) {
  uint8_t *startp;

  startp = bp;
  if (!__ptty_input_read_i(&self->iqueue, &bp, &n) && (bp == startp)) {
    *msgp = MSG_TIMEOUT;
  }

  return (size_t)(bp - startp);
}

/* MIN > 0, TIME = 0, or canonical input with an effective minimum of one.
   The input queue supplies canonical record boundaries in either case.*/
static size_t __ptty_read_blocking_s(hal_posix_tty_sio_c *self,
                                     uint8_t *bp,
                                     size_t n,
                                     msg_t *msgp) {
  uint8_t *startp;
  size_t minimum;

  startp = bp;
  minimum = (self->attributes.c_lflag & ICANON) != 0U ?
            1U : (size_t)self->attributes.c_cc[VMIN];
  while (!__ptty_input_read_i(&self->iqueue, &bp, &n) &&
         ((size_t)(bp - startp) < minimum)) {
    *msgp = __ptty_read_wait_s(self, 0U, TIME_INFINITE);
    if (*msgp != MSG_OK) {
      break;
    }
  }

  return (size_t)(bp - startp);
}

/* MIN = 0, TIME > 0: one deadline starting at read entry.*/
static size_t __ptty_read_timed_s(hal_posix_tty_sio_c *self,
                                  uint8_t *bp,
                                  size_t n,
                                  msg_t *msgp) {
  uint8_t *startp;
  sysinterval_t interval;
  systime_t started;

  startp = bp;
  interval = (sysinterval_t)__ptty_vtime_interval(&self->attributes);
  started = chVTGetSystemTimeX();
  while (!__ptty_input_read_i(&self->iqueue, &bp, &n) && (bp == startp)) {
    *msgp = __ptty_read_wait_s(self, started, interval);
    if (*msgp != MSG_OK) {
      break;
    }
  }

  return (size_t)(bp - startp);
}

/* MIN > 0, TIME > 0: no first-byte deadline, then an inter-byte timer.*/
static size_t __ptty_read_interbyte_s(hal_posix_tty_sio_c *self,
                                      uint8_t *bp,
                                      size_t n,
                                      msg_t *msgp) {
  uint8_t *startp;
  size_t minimum;
  sysinterval_t interval;
  systime_t started;

  startp = bp;
  minimum = (size_t)self->attributes.c_cc[VMIN];
  interval = (sysinterval_t)__ptty_vtime_interval(&self->attributes);
  started = 0U;
  while (true) {
    size_t previous;

    previous = n;
    if (__ptty_input_read_i(&self->iqueue, &bp, &n) ||
        ((size_t)(bp - startp) >= minimum)) {
      break;
    }
    if (n < previous) {
      started = chVTGetSystemTimeX();
    }
    *msgp = __ptty_read_wait_s(self, started,
                               bp == startp ? TIME_INFINITE : interval);
    if (*msgp != MSG_OK) {
      break;
    }
  }

  return (size_t)(bp - startp);
}

/* Called during object initialization or with the system locked.*/
static void __ptty_select_read(hal_posix_tty_sio_c *self) {

  if (((self->attributes.c_lflag & ICANON) != 0U) ||
      ((self->attributes.c_cc[VMIN] > 0U) &&
       (self->attributes.c_cc[VTIME] == 0U))) {
    self->readf = __ptty_read_blocking_s;
  }
  else if (self->attributes.c_cc[VTIME] == 0U) {
    self->readf = __ptty_read_poll_s;
  }
  else if (self->attributes.c_cc[VMIN] == 0U) {
    self->readf = __ptty_read_timed_s;
  }
  else {
    self->readf = __ptty_read_interbyte_s;
  }
}

static size_t __ptty_read(hal_posix_tty_sio_c *self,
                          uint8_t *bp,
                          size_t n,
                          msg_t *msgp) {
  size_t done;

  chDbgCheck((bp != NULL) || (n == 0U));
  *msgp = MSG_OK;
  if (n == 0U) {
    return 0U;
  }

  done = 0U;
  chSysLock();
  if (self->state == HAL_DRV_STATE_READY) {
    done = self->readf(self, bp, n, msgp);
    if (self->iqueue.committed > 0U) {
      __ptty_input_wakeup_i(self);
    }
  }
  else {
    *msgp = MSG_RESET;
  }
  chSchRescheduleS();
  chSysUnlock();

  if ((done == 0U) && (*msgp == MSG_OK)) {
    *msgp = MSG_RESET;
  }
  return done;
}

static msg_t __ptty_drain(hal_posix_tty_sio_c *self) {
  msg_t msg;

  chSysLock();
  if (self->state != HAL_DRV_STATE_READY) {
    chSysUnlock();
    return HAL_RET_INV_STATE;
  }
  while (self->flow_pending ||
         !qIsEmptyI(&self->equeue) ||
         !oqIsEmptyI(&self->oqueue) ||
         sioIsTXOngoingX(self->siop)) {
    self->drain_waiting = true;
    __ptty_push_output_i(self);
    if (!self->drain_waiting) {
      continue;
    }
    msg = chThdEnqueueTimeoutS(&self->drainsync, TIME_INFINITE);
    /* Completion/reset owns drain_waiting. Another caller may already
       have started a new drain before this waiter gets to run again.*/
    if (msg != MSG_OK) {
      chSysUnlock();
      return HAL_RET_INV_STATE;
    }
    if (self->state != HAL_DRV_STATE_READY) {
      chSysUnlock();
      return HAL_RET_INV_STATE;
    }
  }

  __ptty_update_tx_i(self);
  chSchRescheduleS();
  chSysUnlock();

  return HAL_RET_SUCCESS;
}

static void __ptty_apply_attributes_i(hal_posix_tty_sio_c *self,
                                      const struct termios *attrp) {
  bool was_canonical;

  was_canonical = (self->attributes.c_lflag & ICANON) != 0U;
  self->attributes = *attrp;
  __ptty_select_read(self);

  if (was_canonical && ((attrp->c_lflag & ICANON) == 0U)) {
    __ptty_input_commit_i(self);
  }
  __ptty_input_wakeup_i(self);
}
]]></verbatim>
    </types>
  </private>
</module>
